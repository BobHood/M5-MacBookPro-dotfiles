# macOS metadata
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Vim session history (not a config file)
.viminfo

# Outdated scripts (superseded by Brewfile)
.superbrew.sh

# Git submodule tracking (not needed for this repo)
.gitmodules
